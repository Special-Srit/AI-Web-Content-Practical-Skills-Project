# macOS
.DS_Store
._*

# editors
.vscode/
.idea/
*.swp

# node (only if a build step gets added later)
node_modules/
package-lock.json

# secrets / local config
.env
.env.local

# AI tool local state — but keep role agent definitions version-controlled
.claude/*
!.claude/agents/
.aider*

# scratch
tmp/
*.log
